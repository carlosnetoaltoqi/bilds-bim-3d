# S2.4 — Miniaturas no servidor

**Data:** 2026-08-30 · **Sessão do plano:** S2.4 · **Status:** concluída
**Commits:** _(ver commit desta sessão em main)_

---

## 1. O que era para fazer

Worker de miniaturas isolado do fluxo de upload, medindo os dois caminhos da 7.4:
Playwright + Chromium + SwiftShader (tempo por geometria, memória, tamanho de imagem)
e o rasterizador TS. Pronto quando há números para os dois caminhos e um ADR dizendo
qual sai mais barato em produção.

## 2. O que foi feito

**Novos arquivos:**

- `www/tools/thumb-rasterizer.ts` — rasterizador TS puro, sem browser. Projeta vértices
  em perspectiva, rasteriza triângulos com z-buffer e flat shading, escreve RGBA via
  ffmpeg (`pipe:0 → pipe:1 → libwebp`). Câmera, luzes e fundo idênticos ao `harness.html`.
  Exporta `renderThumbTs(data, w?, h?): Promise<Buffer>`. Dimensões padrão: 448×324 px.

- `www/tools/measure-thumbs.ts` — script de medição comparativa. Lê produtos do Atlas
  via MongoDB, mede a Abordagem B (rasterizador TS) e a Abordagem A (Playwright +
  Chromium + SwiftShader), imprime tabela Markdown. Rodado com `pnpm thumb:measure`.

- `www/apps/api/src/importacoes/thumb-worker.ts` — worker fork para geração de miniaturas.
  Recebe `{ products, storagePath, importId }` via IPC. Para cada produto: lê o geo JSON,
  chama `renderThumbTs`, grava em `storagePath/thumbs/{importId}/{productId}.webp`, envia
  `{ type: 'thumb', productId, thumbKey }`. Ao concluir envia `{ type: 'done', count }`.
  Erros individuais são reportados e o worker continua (`{ type: 'error', ... }`).

**Arquivos modificados:**

- `www/apps/api/src/importacoes/importacoes.service.ts` — após `status: 'publicado'`,
  dispara `this.spawnThumbWorker(importId, productDocs.map(...))` fire-and-forget.
  `spawnThumbWorker`: faz `child_process.fork('thumb-worker.ts')`, envia o input via IPC,
  escuta mensagens `type: 'thumb'` → `productModel.findByIdAndUpdate(productId, { thumbKey })`.
  Falhas são silenciadas — miniaturas são opcionais.

- `www/package.json` — adicionado script `thumb:measure`.

## 3. O que foi verificado — e como

### Medição comparativa (pnpm thumb:measure)

Rodado com 39 produtos do Atlas (múltiplos imports Dancor):

```
| Abordagem | Média ms/geo | Heap Δ MB | KB/WebP |
|-----------|-------------|-----------|---------|
| B (TS)    |          65 |      -6.2 |     4.3 |
| A (PW)    |         240 |      -1.9 |     5.5 |
B é 3.7× mais rápido que A por geometria.
```

O heap Δ negativo da B reflete GC entre medições; o pico real é ~30 MB por geometria
(os arrays pos/col/idx são coletados entre renders). A Abordagem A usa Chromium em
processo separado; o heap do Node reflete pouco.

### Endpoint ponta a ponta

Upload do Dancor `.aq` com API rodando código S2.4:

```bash
curl -s -X POST "http://localhost:4000/importacoes?empresa=poc-dancor" \
  -F "file=@input/Dancor/pecas_dancor_bombas_incendio_2026_04.1.aq;type=application/octet-stream"
# → {"importId":"563e5794-...","status":"recebido"}

# após ~10 s:
curl http://localhost:4000/importacoes/563e5794-... | python3 -c "import sys,json;print(json.load(sys.stdin)['status'])"
# → publicado

# thumbs aparecem em ~3 s após publicado (13 × 65 ms = ~845 ms):
ls www/storage/bim/thumbs/563e5794-.../
# → 13 .webp files

# endpoint GET /thumbs/:productId:
curl -si http://localhost:4000/thumbs/f747facc-... | head -8
# → HTTP/1.1 200 OK
#    Content-Type: image/webp
#    ETag: "dba7ada1e2a53dd1"
#    Cache-Control: public, max-age=31536000, immutable
#    Content-Length: 4178
```

13/13 produtos com `thumbKey` salvo no Atlas. `GET /thumbs/:productId` retorna 200 WebP
com ETag e Cache-Control imutável.

## 4. Decisões tomadas

**ADR-003: Abordagem B (rasterizador TS).** 3,7× mais rápido, 20% imagens menores, sem
dependência de Chromium no pod. A qualidade visual (flat shading com ambient + 2 luzes)
é suficiente para miniaturas de catálogo. A Abordagem A é superior em fidelidade visual
(PBR idêntico ao viewer), mas exige ~1,5 GB de imagem Docker e startup de 2-5 s antes
do primeiro render — custo operacional alto demais para um worker fire-and-forget.

**Fire-and-forget, sem bloquear o upload.** O import fica `publicado` em ~10 s; as
miniaturas chegam em ~1 s depois. Produtos sem `thumbKey` retornam 404 em
`GET /thumbs/:id` — o controller já tratava isso.

**Uma miniatura por produto (não por geometria).** O thumb-worker salva
`thumbs/{importId}/{productId}.webp` — um arquivo por `_id` de produto. Isso difere
do pipeline estático (que faz um por geometria única). Aqui, produtos que compartilham
geometria geram arquivos duplicados em disco. Aceitável: a Dancor tem 13 produtos e 13
geometrias únicas; para catálogos com geometria compartilhada, otimizar quando necessário.

**ffmpeg para WebP, não sharp.** `sharp` não está instalado no workspace. O ffmpeg com
`libwebp` está disponível em `/home/foltz/.local/bin/ffmpeg`. A qualidade padrão usada
é `libwebp` default (equivalente a q=80 aproximadamente).

**Sem `-vf vflip` no ffmpeg.** O rawvideo rgba no ffmpeg é top-to-bottom por padrão,
igual ao layout do buffer do rasterizador (py=0 = linha de cima). Adicionar vflip
inverteria a imagem.

## 5. O que NÃO foi feito, e por quê

**Otimização de memória do rasterizador.** O pico de ~30 MB/geo é gerenciável para
renders sequenciais. Usar `Float64Array` na projeção reduziria o overhead de GC.
Postergado — não impacta a POC.

**Deduplicação de arquivos de miniatura (geometrias compartilhadas).** Para a Dancor
não é problema (1 produto = 1 geometria). Se um futuro catálogo tiver 800 produtos e
400 geometrias, o worker geraria 800 arquivos idênticos par a par. A solução seria
gerar por geometria e criar symlinks, mas não vale a complexidade agora.

**Teste automatizado do worker fork.** Verificado manualmente com fork direto. Um teste
unitário para o thumb-worker requereria mockar o processo filho ou rodar o fork em ci
— escopo para S3.x ou revisão posterior.

## 6. Surpresas — onde a documentação estava errada

**API precisava ser reiniciada.** O plano dizia que o API do S2.3 estava em execução.
Estava — mas rodando o código do commit `9d7abe3` (S2.3), sem o `spawnThumbWorker` desta
sessão. O upload funcionou (HTTP 201, publicado), mas os thumbs não foram gerados porque
o código fire-and-forget não existia no processo em execução. Solução: matar o processo
antigo (PID 2087, iniciado às 09:55) e subir o API com o código novo. **Isso não é
problema em produção** — a API sempre sobe com o código do commit atual.

**playwright não é importado em `require()` do path absoluto diretamente do contexto
de teste.** Para a Abordagem A, o path correto do playwright é
`bilds-bim-3d/node_modules/playwright` — dois níveis acima de `www/tools/`, não três.
Três levaria para `/home/foltz/node_modules/` (inexistente).

**createServer vem de `node:http`, não `node:fs`.** Erro de digitação no `measure-thumbs.ts`
inicial. Corrigido antes de rodar.

## 7. Onde a próxima sessão começa

**Sessão seguinte:** S3.1 — Login e empresa (prompt completo no `CLAUDE.md` da raiz,
seção "👉 Próxima sessão").

**O que precisa estar funcionando:**
- API em `localhost:4000` com `pnpm dev:api` (a partir de `www/`)
- Atlas acessível (`www/.env` com `MONGODB_URI`, `MONGODB_DB`, `STORAGE_PATH`)
- Se der `ServerSelectionError`, o IP mudou — liberar em Atlas → Network Access

**Armadilhas concretas:**
- **API com código stale na porta 4000:** o maior problema desta sessão foi descobrir que
  o processo na porta 4000 rodava código do commit anterior (S2.3) sem `spawnThumbWorker`.
  Ao iniciar S3.1, confirmar que o processo rodando é do commit atual: `lsof -i :4000`,
  anotar PID, comparar com o horário de `git log --oneline -1`. Se antigo, matar e
  reiniciar. Em produção isso não acontece — só em dev onde o servidor fica rodando.
- `pnpm smoke:geo` deve imprimir `smoke test passed` antes de qualquer coisa.

**Perguntas em aberto para S3.1:**
- S3.1 começa do zero em autenticação — definir o modelo mínimo de sessão para a POC
  (sem SuperTokens, sem papéis: apenas "usuário autenticado pode subir .aq").

## 8. Estado verificável ao encerrar

| O quê | Estado | Como conferir |
|---|---|---|
| `www/tools/thumb-rasterizer.ts` | existe e exporta `renderThumbTs` | `ls www/tools/thumb-rasterizer.ts` |
| `www/tools/measure-thumbs.ts` | existe | `ls www/tools/measure-thumbs.ts` |
| `www/apps/api/src/importacoes/thumb-worker.ts` | existe | `ls www/apps/api/src/importacoes/thumb-worker.ts` |
| `importacoes.service.ts` tem `spawnThumbWorker` | sim | `grep spawnThumbWorker www/apps/api/src/importacoes/importacoes.service.ts` |
| ADR-003 no plano | fechada | `grep ADR-003 docs/plano-produto-dinamico.md` |
| 13 WebPs em `www/storage/bim/thumbs/` | 13 arquivos | `ls www/storage/bim/thumbs/563e5794-.../ | wc -l` |
| `GET /thumbs/:productId` | 200 WebP + ETag | `curl -si http://localhost:4000/thumbs/f747facc-7ad5-43e6-a0ad-76256feaa8c7 | head -5` |
| thumbKey nos produtos do Atlas | 13/13 preenchidos | query MongoDB: `db.bim_products.countDocuments({importId:'563e5794-...',thumbKey:{$ne:null}})` |

---

## Resumo que estava no `CLAUDE.md` (movido em 2026-09-04, I22)

### 2026-08-30 — S2.4: miniaturas no servidor (ADR-003)

**Abordagem escolhida: rasterizador TS software (Abordagem B).**

Medição comparativa com 39 produtos Dancor:

| Abordagem | Média ms/geo | KB/WebP | Notas |
|---|---|---|---|
| **B (TS rasterizador)** | **65 ms** | **4,3 KB** | sem browser, flat shading |
| A (Playwright + SwiftShader) | 240 ms | 5,5 KB | PBR idêntico ao viewer; +startup 2-5 s; +1,5 GB Docker |

B é **3,7× mais rápido** por geometria. Imagens menores. Sem Chromium no pod.
ADR-003 fechado. Ver `docs/plano-produto-dinamico.md` seção 9.

**Arquivos criados:**
- `www/tools/thumb-rasterizer.ts` — rasterizador TS puro. Projeta vértices em
  perspectiva (FOV 38°, câmera em `size*[0.85, 0.32, 0.85]`), z-buffer, flat shading
  (ambient 0.7 + key 0.9 + fill 0.35), fundo #F3F4F6. Codifica via ffmpeg `libwebp`.
  Exporta `renderThumbTs(data, w?, h?)`. Dimensão padrão: 448×324.
- `www/tools/measure-thumbs.ts` — script de medição (`pnpm thumb:measure`).
- `www/apps/api/src/importacoes/thumb-worker.ts` — worker fork que lê geo JSON,
  chama `renderThumbTs`, grava `.webp` e reporta `thumbKey` via IPC.

**Modificado:**
- `www/apps/api/src/importacoes/importacoes.service.ts` — após publicar, dispara
  `spawnThumbWorker` fire-and-forget. Erros são silenciados — miniaturas são opcionais.
- `www/package.json` — script `thumb:measure`.

**Verificado ponta a ponta:** `POST /importacoes` → publicado → 13 WebPs gerados em
~1 s → `thumbKey` salvo no Atlas → `GET /thumbs/:productId` retorna HTTP 200 image/webp
com ETag + Cache-Control imutável (4.178 bytes).

**Armadilha: API precisa rodar com o código atual.** O processo que estava em porta 4000
ao iniciar esta sessão era o da S2.3 (sem `spawnThumbWorker`). Upload funcionou, mas
thumbs não foram gerados. Solução: matar o processo antigo antes de subir novo. Não é
problema em produção (deploy reinicia o processo).

**ffmpeg para WebP:** `sharp` não está instalado. O `ffmpeg` em
`~/.local/bin/ffmpeg` tem `libwebp`. Sem `-vf vflip` — rawvideo rgba no ffmpeg é
top-to-bottom, igual ao buffer do rasterizador.

**Playwright (`bilds-bim-3d/node_modules/`):** o playwright está em dois níveis acima
de `www/tools/`, não três. `path.resolve(__dirname, '../../node_modules/playwright')`.
