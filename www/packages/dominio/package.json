{
  "name": "@bim/dominio",
  "version": "0.0.1",
  "private": true,
  "description": "O que a API de catálogo e o serviço de ingestão compartilham: schemas Mongoose, storage em disco, validação de geometria/DTOs, ETag e nome de upload. Fonte TypeScript direto (main = src/index.ts): cada app compila junto.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/mongoose": "^11.0.4",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "mongoose": "^9.9.4",
    "reflect-metadata": "^0.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
