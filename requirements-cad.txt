# Opcionais — só para o que segue. Versões pinadas nas que rodaram nesta máquina
# (Ubuntu 24.04, Python 3.12.3, 2026-09-04). Instale FORA do pipeline padrão:
#
#   pip install --user --break-system-packages -r requirements-cad.txt
#   (PEP 668: o Ubuntu exige a flag; ou use um venv)
#
-r requirements.txt
ifcopenshell==0.8.5      # www/apps/ingestao/pipeline/ifc_to_geo.py (IFC B-rep e IFC grande na POC); round-trip do exportador IFC do editor
cadquery-ocp==7.9.3.1.1  # OpenCASCADE: www/apps/ingestao/pipeline/step_to_geo.py (tesselar .stp e .igs — IGES costurado desde a S7.17); ~165 MB
pypdf==6.14.2            # eng-reversa/tools/pdf_coords.py (catálogo comercial em PDF → células)
olefile==0.47            # www/apps/ingestao/pipeline/rfa_partatom.py (tipos de uma família Revit .rfa, para o import de plugin de AutoCAD); sem ele o import segue sem a spec "Tipos Revit"
