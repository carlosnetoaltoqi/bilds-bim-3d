# Opcionais — só para o que segue. Versões pinadas nas que rodaram nesta máquina
# (Ubuntu 24.04, Python 3.12.3, 2026-09-04). Instale FORA do pipeline padrão:
#
#   pip install --user --break-system-packages -r requirements-cad.txt
#   (PEP 668: o Ubuntu exige a flag; ou use um venv)
#
-r requirements.txt
ifcopenshell==0.8.5      # bim_pipeline.conversores.ifc (IFC B-rep e IFC grande na POC); round-trip do exportador IFC do editor
cadquery-ocp==7.9.3.1.1  # OpenCASCADE: bim_pipeline.conversores.step_iges (tesselar .stp e .igs — IGES costurado desde a S7.17); ~165 MB
pypdf==6.14.2            # eng-reversa/tools/pdf_coords.py (catálogo comercial em PDF → células)
# olefile (famílias Revit .rfa) deixou de ser opcional: é dependência da biblioteca (requirements.txt / pyproject)
