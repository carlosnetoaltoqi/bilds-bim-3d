{
  "name": "@bim/base",
  "version": "0.1.0",
  "private": true,
  "description": "Infraestrutura comum dos serviços Nest: processo filho, BibliotecaCli (a porta para o Python), upload em disco, ValidationPipe, download em stream, bootstrap. Compilado para dist/ (project references).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b -w",
    "clean": "tsc -b --clean && rm -rf dist"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "dotenv": "^16.0.0",
    "multer": "^2.3.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.0.0",
    "@types/multer": "^2.2.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
