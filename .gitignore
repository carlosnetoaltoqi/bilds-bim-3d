input/

# Saída do modo lote (`python3 -m bim_pipeline.cli.zip_bilds --all`) e da suíte — tudo regerável a partir do .aq.
output/**
!output/.gitkeep

node_modules/

__pycache__/
*.pyc
*.pyo
.env
.env.*
**/.env
**/.env.*
!**/.env.example
config.json
.vercel

# POC — www/
.next/
www/**/dist/
www/storage/
**/.venv/
*.tsbuildinfo

# Estudos arquivados: downloads de catálogo web (dezenas de MB), saídas geradas e o formulário de lead (dados pessoais)
docs/historico/estudos/*/downloads/
docs/historico/estudos/*/saida/
**/lead.local.json

.pytest_cache/

# Fixtures reais por papel (ADR-016): quais arquivos desta máquina fazem cada papel
tests/fixtures.local.json
