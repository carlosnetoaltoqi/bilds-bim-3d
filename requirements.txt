# A biblioteca comum (pacote bim_pipeline, em biblioteca/) em modo editável — traz numpy e olefile (famílias Revit .rfa). Testes: requirements-dev.txt.
# STEP/IGES/IFC/PDF: requirements-cad.txt (ou `pip install -e 'biblioteca[cad]'`).
-e ./biblioteca
